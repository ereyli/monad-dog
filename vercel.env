# Vercel Environment Variables
# Bu dosyayı Vercel Dashboard'da Environment Variables bölümüne manuel olarak ekleyin

# Supabase Configuration
SUPABASE_URL=https://uhqszfoekqrjtybrwqzt.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Environment
NODE_ENV=production

# Rate Limiting (Production settings)
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX_REQUESTS=100

# Security
JWT_SECRET=a2fHSKxZyAdywZqR5GvSt8pPmE5g1OclmNiyTMfEUNE=

# Blockchain Configuration (if needed)
MONAD_RPC_URL=https://testnet-rpc.monad.xyz
MONAD_CHAIN_ID=10143

# ⚠️  IMPORTANT: 
# 1. Bu dosyayı Vercel Dashboard'da Environment Variables bölümüne ekleyin
# 2. SUPABASE_ANON_KEY değerini gerçek Supabase anon key'iniz ile değiştirin
# 3. JWT_SECRET değerini güvenli bir random string ile değiştirin
# 4. Bu dosyayı git'e commit etmeyin (sensitive data içerir) 